using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrampolineCheckController : MonoBehaviour
{

   /* [SerializeField]
    float TrampolinePower = 15;

    PlayerControllerEForTesting _player;
    // Start is called before the first frame update
    void Start()
    {
        _player = transform.parent.gameObject.GetComponent<PlayerControllerEForTesting>();
    }

    // Update is called once per frame
    private void OnTriggerEnter(Collider other)
    {
        Debug.Log("´ê¾Ò´Ù " + other.gameObject.name);
        if (other.gameObject.CompareTag("Trampoline"))
        {
            Rigidbody _rb = _player.GetComponent<Rigidbody>();
            _rb.AddForce(Vector3.up * TrampolinePower, ForceMode.Impulse);
        }

    }*/
}
